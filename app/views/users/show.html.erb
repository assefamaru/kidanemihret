<% provide(:title, @user.name) %>

<section id="user-profile">
    <div class="control-width">
        <div class="control-width-inner">
            <h1>
              <%= gravatar_for @user %>
              <%= @user.name %>
            </h1>

            <p><%= @user.email %></p>
        </div>
    </div>
</section>
